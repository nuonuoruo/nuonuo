# 安全说明

这个项目目前是静态网页，可以直接部署到 GitHub Pages。静态网页里的 JavaScript 会被浏览器下载，所以写在前端代码里的 COS `SecretId` 和 `SecretKey` 无法真正保密。

已经完成的加固：

- 新注册账号不再保存明文密码，改为 `PBKDF2-SHA256` 加盐哈希。
- 旧账号仍可登录；第一次成功登录后会尝试自动升级为哈希记录。
- 登录框增加了基础的自动填充和拼写控制属性。

仍然存在的结构性风险：

- COS 密钥仍在前端代码中，任何访问网页的人都能看到。
- 真正安全的做法是改成后端签发临时密钥，或使用腾讯云 STS/COS 临时授权，并把长期密钥只放在服务器环境变量里。

